# Arm D results — conformance of the verifier on a synthetic grid

> ## What arm D is
>
> **Arm D is a pre-registered, same-author synthetic conformance corpus scored
> against author-declared labels.**
>
> **Its 90-row confusion matrix is exact for that finite grid, but it is not
> independently labelled or policy-blind and does not establish transferable
> recall, precision, or false-positive cost.**
>
> It is an engineering regression and conformance suite. It is **not** the metric
> rescue, it does not meet the Track 2 metric bar, and it does not repair arm C.
> Arm C's failed agent-trace result stands unchanged in
> `PRELABEL-FINDING-armC.md`.
>
> ### Why the original framing was wrong
>
> **The scorer never reads the intent.** `cmd/rzp-armd/main.go` branches on
> `r.Label`, a field authored into `requests.json`. `intent_text` is loaded and
> never used in a decision, and `intent_payment` is not even in the struct. A
> one-field edit to `label` changes the reported precision and recall. The claim
> "ground truth comes from intent, never the mandate" was false **as an
> implementation claim**, whatever the generator intended.
>
> **Freezing the policy first does not blind the author.** The same person who
> knew the decision rule constructed the corpus afterwards. A date proves the
> code was not fitted to the data; it does not make the data independent of the
> author's knowledge of the code.
>
> **D1 is tautological.** Every positive was constructed as an unmatched amount
> or an unmatched payment. A default-deny capability verifier must refuse all of
> them. Recall 1.000 restates the construction; it does not measure detection.
>
> **The false-positive labels are not settled ground truth.** An intent reading
> "refund the item price, 24,000 paise" does not self-evidently mean a
> 12,000-paise partial refund is in-intent. The six `coverage=exact /
> request=under` rows counted as false positives may be a correct exact-
> authorization refusal rather than a merchant-harming block. Deciding that needs
> independent human labels or a separately justified rule, and has neither.
>
> ### The minimum path to a literal Track-qualifying claim
>
> Independent blinded labels for these 90 rows: each rater shown the intended
> payment, the intent, and the requested refund — and **not** the mandate or the
> guard's decision. Until that exists, nothing here is a metric result.



Generated by `rzp-armd`. Computed, not written by hand.

## Scope

A conformance check of a **deterministic verifier** on a synthetic grid.
things Track 2 names. 90 constructed refund requests, each scored against a
merchant intent and the mandate compiled from it.

**Not agent traffic.** The requests are constructed, so nothing here says
how often an agent would make one. Arm C asked that question, failed, and
that failure stands: see `PRELABEL-FINDING-armC.md`.

**Policy frozen beforehand, which is narrower than held out.** `fb87b12`
(2026-08-30), before this corpus existed. The implementation cannot have
been fitted to data that did not exist, and both commits are checkable. No
model is called; nothing is random; the corpus is scored once.

**Ground truth comes from the intent, never the mandate.** The guard decides
from the compiled mandate; the compilation is deliberately lossy. The gap
between them is what this measures — see `PROTOCOL-armD.md` §4.

---

## 1. Confusion matrix

Unit: one refund request. Positive class: **out-of-intent**.
Predicted positive: the guard refused it.

| | guard refused | guard allowed |
|---|---|---|
| **out-of-intent** | TP 54 | FN 0 |
| **in-intent** | FP 17 | TN 19 |

- **Precision 0.761**  (TP / TP+FP)
- **Recall 1.000**  (TP / TP+FN)
- True-positive rate 1.000, false-positive rate 0.472

### Class counts

| | n |
|---|---|
| out-of-intent | 54 |
| in-intent | 36 |
| total | 90 |

---

## 2. Precision is base-rate dependent; recall is not

Prediction D5, recorded before scoring. The corpus is 60% positive by
construction, and that is a design choice, not an observed rate. Precision
moves with it. **Recall does not** — it is computed only over positives.

Holding the measured rates fixed (TPR 1.000, FPR 0.472), precision at other
base rates would be:

| out-of-intent share | precision |
|---|---|
| 60% (this corpus) | 0.761 |
| 25% | 0.414 |
| 10% | 0.190 |
| 5% | 0.100 |
| 1% | 0.021 |

**Quote the recall and the false-positive rate, not the precision alone.**
Precision here is a property of how the corpus was built.

---

## 3. False-positive cost

A false positive is an **in-intent request the guard refused** — a refund
the merchant wanted, blocked.

| | |
|---|---|
| False positives | 17 |
| Value withheld | 689000 paise |
| False-positive rate | 0.472 |

### By mandate coverage

| coverage | TP | FP | TN | FN |
|---|---|---|---|---|
| `exact` | 18 | 6 | 6 | 0 |
| `split` | 18 | 1 | 11 | 0 |
| `under` | 18 | 10 | 2 | 0 |

Predictions D3 and D4 said every false positive would fall in
`coverage=under` and none in `exact` or `split`. The table above is the test.

### By request type

| request | TP | FP | TN | FN |
|---|---|---|---|---|
| `exact` | 0 | 7 | 11 | 0 |
| `far_over` | 18 | 0 | 0 | 0 |
| `over` | 18 | 0 | 0 | 0 |
| `under` | 0 | 10 | 8 | 0 |
| `wrong_payment` | 18 | 0 | 0 | 0 |

---

## 4. What this does not establish

- Not a rate of agent misbehaviour. The requests are constructed.
- Not merchant traffic, and not a fraud rate.
- Precision is a property of this corpus's class balance.
- The false-positive cost is a property of the intent→mandate compiler as
  much as of the guard. `coverage=under` cells were built so the mandate
  under-expresses the intent; refusing there is correct enforcement of an
  incomplete authorization.
- One author designed the dimensions. Enumeration removes selection bias
  within the grid, not the grid's blind spots.
